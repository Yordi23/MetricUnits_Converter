# MetricUnits_Converter

The input file should contain three columns.

First column: contains the value to convert.

Second column: contains from which metric unit you want to convert.

Third column: contains the metric unit to which you want to convert.

After starting the program, you must introduce the path where the input file is located. The program will create an output file on the same path as the input file.
